#include "main.h"
/**
 *_islower - check for lower case
 *@c: character to check
 *Return: 1 if caracter is lower
 *Return: 0 otherwise
 *
 */
int _islower(int c)
{
	char alpha;

	if (alpha >= 97 && alpha <= 122)
	{
		return (1);
	}
	return (0)
}
