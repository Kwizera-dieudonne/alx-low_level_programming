#include "main.h"
/**
 *_isupper - check if character is upper case
 *@c: char to be checked
 *Return: 1 if char is upper, 0 otherwise
 */
int _isupper(int c)
{
	if (c >= 'A' && C <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
